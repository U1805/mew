# Override for pulling prebuilt images from GHCR.
# Use with:
# docker compose --env-file docker-compose.env -f docker-compose.yml -f docker-compose.ghcr.yml -f docker-compose.cc.yml -f docker-compose.cc.ghcr.yml up -d
name: mew

services:

  b4u2cc:
    image: "ghcr.io/u1805/mew/b4u2cc:latest"
    build: null

  claude-code:
    image: "ghcr.io/u1805/mew/claude-code:latest"
    build: null
