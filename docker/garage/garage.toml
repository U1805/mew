metadata_dir = "/var/lib/garage/meta"
data_dir = "/var/lib/garage/data"
db_engine = "sqlite"

replication_factor = 1

# Single-node RPC config (dev)
rpc_bind_addr = "[::]:3901"
rpc_public_addr = "garage:3901"
# Dev-only secret. For production, regenerate it (see docs).
# Must be 32 bytes of random hex (64 hex chars).
rpc_secret = "5a9a8d0d6d85f9c22f9f8d60b6c6db62dfb02e2c8b7c3b5d6a6c340f8a5d7e3c"

[s3_api]
s3_region = "garage"
api_bind_addr = "[::]:3900"
root_domain = ".s3.garage.localhost"

[s3_web]
bind_addr = "[::]:3902"
root_domain = ".web.garage.localhost"
index = "index.html"

[admin]
api_bind_addr = "[::]:3903"
# Dev-only token. For production, regenerate it (see docs).
admin_token = "dev-admin-token"
metrics_token = "dev-metrics-token"

